
@{
    ViewBag.Title = "Adicionar Usuário";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h3><i class="fa fa-angle-right"></i> Alteração de Usuário</h3>
<div class="row mt">
    <div class="col-lg-12">
        <div class="form-panel">
            <h4 class="mb"><i class="fa fa-angle-right"></i> Preencha os campos para efetuar o cadastro.</h4>
            @using (Html.BeginForm("Alteracao", "Usuario", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-inline" }))
            {   <fieldset class="form-group">
                <legend>Dados do Usuário</legend>
                @foreach (var user in Model)
                {
                <div class="form-group">
                    <label class="control-label">Nome:</label>
                    <input type="text" class="form-control" value="@user.Nome" id="Nome" name="Nome" required />
                </div>

                <div class="form-group">
                    <label for="exampleInputPassword2">Sobrenome:</label>
                    <input type="text" class="form-control" value="@user.Sobrenome" id="Sobrenome" name="Sobrenome" required />
                </div>

                
                @*<div class="form-group">
                    <label class="checkbox-inline">
                        @if (user.Admin == 0)
                        {
                            <input type="checkbox" id="Admin" name="Admin" value="0"> 
                        }
                        else
                        {
                            <input type="checkbox" id="Admin" name="Admin" value="1" checked="checked">
                        }
                        Administrador
                    </label>
                </div>*@
                    <br /><br />

                <div class="form-group">
                    <label for="exampleInputEmail2">Login:</label>
                    <input type="text" class="form-control" value="@user.Login" id="Login" name="Login" required />

                </div>
                <div class="form-group">
                    <label for="exampleInputPassword2">Senha:</label>
                    <input type="password" class="form-control" value="@user.Senha" id="Senha" name="Senha" required />
                </div>

                <div class="form-group">
                    <label for="exampleInputPassword2">Digite novamente a senha:</label>
                    <input type="password" class="form-control" id="senha2" name="senha2" required>
                </div>
                }
            </fieldset>
               
                <br /><br />
                <div class="form-group">
                    <button type="submit" class="btn btn-success"><i class="fa fa-check"></i> Alterar</button>
                </div>

            }

        </div>
    </div>
</div>